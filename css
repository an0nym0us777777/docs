.navigation {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 70px;
  display: flex;
  align-items: center;
  background-color: var(--background);
  z-index: 1001;
  color: var(--text-primary); 
  transition: none;
  justify-content: space-between;
padding-right: 20px;
  
}

.navigation.open {
  height: auto;
}

.logo {
  margin-left: 25px;
  height: auto;
  width: 35px;
}

.navigation ul {
  list-style: none;
  display: flex;
  align-items: center;
  margin-right: 20px;
  
}

.navigation li {
  margin: 0 20px;
}

.navigation li a {
  text-decoration: none;
  color: var(--nav-text);
  font-size: 16px;
 font-weight: 600;
  transition: none;
}

.navigation li a:hover {
  color: #ccc;
}

.navigation .metamask-btn {
  display: inline-block;
  vertical-align: middle;
}

.hamburger-menu {
  display: none;
  flex-direction: column;
  justify-content: space-between;
  width: 30px;
  height: 20px;
  position: absolute;
  top: 20px;
  right: 20px;
  cursor: pointer;
  z-index: 1001;
}

.hamburger-menu span {
  display: block;
  width: 100%;
  height: 3px;
  background-color: var(--text-primary);
}

.navigation ul:first-of-type {
  flex: 1;
  align-items: center;
}

.metamask-container {
  margin-right: 20px;
  margin-left: auto;
  margin-bottom: 13px;
}

.theme-toggle {
  margin-left: 10px;
  margin-left: auto;
  margin-bottom: 14px;
}
/* Media queries */

@media screen and (max-width: 992px) {
  .navigation {
    justify-content: space-between;
  }

  .navigation ul {
    display: flex;
  }

  .navigation li {
    text-align: left;
  }

  .navigation .metamask-btn {
    margin-left: auto;
  }

  .hamburger-menu {
    display: none;
  }
}


@media screen and (max-width: 768px) {
.navigation {
  justify-content: center;
  
}

.navigation .logo {
  flex: 1;
  margin-top: 16px;
  max-width: 35px;
  height: auto;
  padding-bottom: 11px;
  padding-right: 20px;
  transform: translateZ(0);
}


.navigation ul {
  display: none;
  flex-direction: column;
  position: fixed;
  top: 60px;
  left: 0;
  width: 100%;
  background-color: var(--background);
  z-index: 1000;
  padding: 0;
  margin-top: 0;    
  height: 100vh;
}

.navigation.open {
  height: auto; 
}

.navigation.open ul {
  display: flex;
  flex-direction: column;
  position: fixed;
  top: 60px;
  left: 0;
  width: 100%;
  background-color: var(--background);
  z-index: 1000;
  padding: 0;
  margin-top: 0;
}

.navigation li {
  text-align: center;
  font-size: 22px;
  padding: 25px 0;
  margin-top: 10px;
}

.hamburger-menu {
  position: fixed;
  top: 25px;
  right: 20px;
  display: flex;
}
.hamburger-menu span {
  background-color: var(--text-primary); 
  }
}

.theme-toggle i {
font-size: 2rem;
cursor:pointer
}

.extras-container {
  display: flex;
  align-items: center;
  margin-right: 25px;
  margin-top: 15px;
}

@media screen and (max-width: 768px) {
  .extras-container {
    display: none;
  }

  .navigation.open .extras-container {
    display: flex;
    flex-direction: row;
    position: fixed;
    top: auto; /* Changez la valeur de 'top' à 'auto' */
    bottom: 0; /* Ajoutez cette ligne pour positionner les éléments au bas du menu */
    background-color: var(--background);
    padding: 15px;
    justify-content: center;
    align-items: center;
    z-index: 2000;
  margin-right: 0px;
  }

}
